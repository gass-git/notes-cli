{
  "version": "1.0.23",
  "url": "https://github.com/gass-git/notes-cli/releases/download/v1.0.23/notes-cli.exe",
  "description": "A simple command-line tool to manage and keep track of your notes.",
  "homepage": "https://github.com/gass-git/notes-cli",
  "license": "https://spdx.org/licenses/FTL.html",
  "hash": "0019dfc4b32d63c1392aa264aed2253c1e0c2fb09216f8e2cc269bbfb8bb49b5",
  "post_install": [
    "# Create a directory to store data",
    "$path = Join-Path $env:APPDATA 'notes-cli'",
    "if (-not (Test-Path $path)){New-Item -ItemType Directory -Path $path | Out-Null}",
    "Write-Host 'Do you want notes-cli to open in a cmd window automatically after boot? (Y/N)' -ForegroundColor Yellow",
    "$answer = Read-Host",
    "if ($answer -match '^(Y|y)') {",
    "  $startup = \"$env:APPDATA\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\"",
    "  $shortcut = Join-Path $startup 'notes-cli.lnk'",
    "  $ws = New-Object -ComObject WScript.Shell",
    "  $s = $ws.CreateShortcut($shortcut)",
    "  $s.TargetPath = 'cmd.exe'",
    "  $s.Arguments = '/k notes-cli show'",
    "  $s.WorkingDirectory = (Get-Location).Path",
    "  $s.Save()",
    "  Write-Host 'Shortcut created in Startup!' -ForegroundColor Green",
    "}"
  ],
  "post_uninstall": [
    "# Removes notes-cli.lnk startup shortcut if it exists",
    "$startupPath = \"$env:APPDATA\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\"",
    "$shortcutPath = Join-Path $startupPath 'notes-cli.lnk'",
    "if (Test-Path $shortcutPath){",
    "   Remove-Item $shortcutPath -Force",
    "   Write-Host 'notes-cli automatic open on boot has been disabled' -ForegroundColor Green",
    "}"
  ],
  "bin": "notes-cli.exe"
}
